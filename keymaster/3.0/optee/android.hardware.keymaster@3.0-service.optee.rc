service keymaster-3-0 /vendor/bin/hw/android.hardware.keymaster@3.0-service.optee
    class early_hal
    user root
    group system drmrpc shell tee tee_device
    seclabel u:r:su:s0
    #seclabel is still NEEDED to autostart even if user root
